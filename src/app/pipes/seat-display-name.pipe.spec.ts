import { SeatDisplayNamePipe } from './seat-display-name.pipe';

describe('SeatDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SeatDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
